<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">

	<head>
		<meta http-equiv="content-type" content="text/html;charset=utf-8"/>
		<title><%= h SITE_TITLE %> | <%= h @page_title %></title>
		<%= stylesheet_link_tag "quorum" %>
		<%= @extra_headers %>
	</head>

	<body>
		<p>
			<%= content_tag :span, h(SITE_TITLE), :class => :siteName %> |
			<%= link_to h(_("List events")), url_for(:controller => :event, :action => :list) %> |
			<%= link_to h(_("Add event")), url_for(:controller => :event, :action => :new) %> |
			<%= link_to h(_("User profile")), profile_path %> |
			<% if User.current_user.role.name == 'admin' -%>
			  <%= link_to h(_("Contact list")), list_users_path %> |
			<% end -%>
			<%= link_to h(_("Log out")), url_for(:controller => :logout) %> <%- if false %> Is there a better way to generate that URL? <% end %>
		</p>
		<p><%= _("Welcome, %s!" % content_tag(:span, h(current_user.fullname), :class => :name)) %></p>
		<%= content_tag(:div, flash[:notice], :class => :flash_notice) if !flash[:notice].nil? %>
		<%= content_tag(:div, flash[:warning], :class => :flash_warning) if !flash[:warning].nil? %>
		<%= content_tag(:div, flash[:error], :class => :flash_error) if !flash[:error].nil? %>
		<h1><%= h @page_title %></h1>
		<%= yield %>
		
		<p class="footer"><a href="http://validator.w3.org/check?uri=referer"><img
        src="http://www.w3.org/Icons/valid-xhtml10"
        alt="Valid XHTML 1.0 Transitional" height="31" width="88" /></a></p>
	</body>

</html>